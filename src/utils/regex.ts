// Regexes from api.yaml spec
export const retailerRegex = /^[\w\s\-&]+$/;
export const dateRegex = /^\d{4}-\d{2}-\d{2}$/;
export const timeRegex = /^\d{2}:\d{2}$/;
export const moneyRegex = /^\d+.\d{2}$/;
